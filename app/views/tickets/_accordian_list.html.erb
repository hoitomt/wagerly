<div id="ticket-container" data-tickets="<%= tickets.as_json(methods: [:description], include: {ticket_tags: {methods: :tag_name} }).to_json %>" data-clients="<%= current_user.clients.as_json(methods: :full_name).to_json %>"></div>
